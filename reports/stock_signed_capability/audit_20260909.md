# Stock capability revalidation, 2026-09-09

This pass fulfills the supplied SocketCommandSource/KIM19 research request using
the existing committed work, a fresh full Java index, and focused source checks.
It does not represent a new target observation or a newly discovered listener.
The current conclusions and ranked candidates are at the beginning of
[the focused report](../../docs/stock_signed_capability_analysis.md).

## Isolation and lineage

The original checkout was inspected first. It had 40 tracked edits and one
untracked status document. A new worktree and branch were created from the
specified completed network-probe commit:

```text
git worktree add E:/Documents/GitHub/jeep_uconnect_custom_stock_signed_audit -b codex/stock-signed-capability-audit be5a83be9fc52493ed04070abac12531c6c6b901
```

Existing committed research was discovered on other branches. Its relevant
completed lineage was adopted with `git merge --ff-only
5b9826b2b6537f12b4bf08a50003ea6914263700` in the new worktree only. No other
worktree was switched, cleaned, stashed, reset, restored, staged, or edited.
No merge into main was performed. The starting commit and adopted evidence
checkpoint are deliberately recorded separately.

The added audit files are this record and `audit_20260909.json`. The two modified
documents are `docs/stock_signed_capability_analysis.md` and
`docs/kim19_final_capability_assessment.md`. Existing tools and inventories are
inherited unchanged. The JSON includes the complete inherited file list relative
to the requested network-probe start. The final SHA is reported after committing
and verified against the pushed branch; a commit cannot embed its own SHA.

## Fresh checks and limits

| Check | Fresh result |
|---|---|
| Analysis unit-test discovery | 305 passed; zero failures/errors |
| Full stock bytecode index | 300 JARs, 72,507 class occurrences, 13,272 unique class hashes, no parse failure or skipped link |
| Index comparison | All JSON content and 300 archive hashes equal the committed evidence |
| Independent JDK 8 disassembly | SocketCommandSource and CommandSource constructors, bind, accept, read/write and cleanup confirmed |
| Focused parser inspection | Looper constructor/start/stop/run calls and all 16 translator command literals/handler call sites confirmed |
| KIM19 final generator | Three reports and their hash-bound evidence inputs verified |
| KIM19 Yelp generator | Seven reports verified; recovered sources unchanged |
| KIM19 runtime generator | Eight reports verified; recovered sources unchanged; 19 JARs / 4,572 classes |
| Yelp observation generator | Check mode exited 0 |
| Performance producer, consumer and native-storage generators | Each check mode exited 0 |
| Original checkout preservation | HEAD, branch, 41 dirty paths, status/diff hashes and untracked document blob match the existing 2026-09-08 checkpoint |

The regenerated index has CRLF line endings on this host; the committed blob
has LF. JSON equality and equality after CRLF normalization both pass. Raw byte
equality does not pass and is explicitly false in the JSON. No evidence inventory
was rewritten to conceal this formatting difference. Fresh/committed hashes are
recorded separately. No analyzer implementation changed, so the tests exercise
the inherited parsing, evidence-boundary, malformed-input, synthetic-fixture and
deterministic-report behavior. Tests establish tooling behavior, not radio state.

The original snapshot comparison uses `GIT_OPTIONAL_LOCKS=0` and hashes the
outputs of `git status --porcelain=v2 --branch`, `git diff --no-ext-diff --binary`,
and `git diff --cached --no-ext-diff --binary`. It also compares `git rev-parse
HEAD`, the branch, and `git hash-object docs/00_project_status.md` against
[the prior preservation record](../kim19_final/verification.md).

## Reproduction

Run from this isolated worktree. Paths below point to existing read-only inputs;
only the regenerated index is written, under this worktree's ignored
`analysis_work` directory. These commands parse artifacts; they do not load
recovered Java classes or execute installer/native code.

```powershell
$env:PYTHONDONTWRITEBYTECODE = '1'
$env:GIT_OPTIONAL_LOCKS = '0'
$auditPython = 'E:/Documents/GitHub/jeep_uconnect_custom/analysis_work/post_reboot_20260906/venv/Scripts/python.exe'
$auditWork = 'E:/Documents/GitHub/jeep_uconnect_custom/analysis_ra4_18.45.01/work'
$auditJavap = 'C:/Program Files/Eclipse Adoptium/jdk-8.0.504.1-hotspot/bin/javap.exe'
$auditXlets = "$auditWork/secondary_iso/usr/share/XLETS"
$auditYelp = "$auditXlets/kim_packages/KIM19/xlets/1D5347C0-8B5E-11E2-9E96-0800200C9A66/prog/jars/640X480_84_Yelp_v03.00.33_FIT.jar"
$auditIndex = Get-Content reports/stock_signed_capability/bytecode_index.json -Raw | ConvertFrom-Json

& $auditPython -m unittest discover -s analysis_tools/tests -p 'test_*.py'
if ($LASTEXITCODE -ne 0) { throw 'analysis tests failed' }
& $auditPython -m analysis_tools.stock_capability_index --root $auditXlets --select $auditIndex.selection_regex --output analysis_work/stock_signed_audit/bytecode_index.json
if ($LASTEXITCODE -ne 0) { throw 'full index failed' }
& $auditPython -m analysis_tools.kim19_final_capability --output reports/kim19_final --check
if ($LASTEXITCODE -ne 0) { throw 'final evidence check failed' }
& $auditPython -m analysis_tools.kim19_yelp_analysis --work $auditWork --output reports/kim19_yelp --check
if ($LASTEXITCODE -ne 0) { throw 'Yelp source check failed' }
& $auditPython -m analysis_tools.kim19_runtime_analysis --work $auditWork --output reports/kim19_runtime_analysis --javap $auditJavap --check
if ($LASTEXITCODE -ne 0) { throw 'runtime evidence check failed' }
& $auditPython -m analysis_tools.yelp_observation_analysis --app-manager "$auditWork/hidden_hbc_ifs/segment_00f20000/files/bin/appManager" --yelp-jar $auditYelp --check
if ($LASTEXITCODE -ne 0) { throw 'observation model check failed' }
& $auditPython tools/analyze_performance_pages_export.py --corpus $auditWork --check
if ($LASTEXITCODE -ne 0) { throw 'Performance producer check failed' }
& $auditPython -m analysis_tools.performance_pages_consumer_census --corpus $auditWork --check
if ($LASTEXITCODE -ne 0) { throw 'Performance consumer check failed' }
& $auditPython -m analysis_tools.performance_pages_native_storage --corpus $auditWork --check
if ($LASTEXITCODE -ne 0) { throw 'native storage check failed' }
& $auditJavap -classpath "$auditXlets/kim_packages/KIM3/xlets/c1d77320-6335-48b2-aa22-21912f657311/prog/jars/c1d77320-6335-48b2-aa22-21912f657311.jar" -c -p -s com.tweddle.test.input.SocketCommandSource com.tweddle.test.input.CommandSource
if ($LASTEXITCODE -ne 0) { throw 'independent disassembly failed' }
```

Compare parsed JSON, not raw formatting, using Python:

```python
import json
from pathlib import Path
fresh = Path('analysis_work/stock_signed_audit/bytecode_index.json').read_bytes()
committed = Path('reports/stock_signed_capability/bytecode_index.json').read_bytes()
assert json.loads(fresh) == json.loads(committed)
assert fresh.replace(b'\r\n', b'\n') == committed.replace(b'\r\n', b'\n')
```

## Interpretation corrections

- The provided part number is known; installed/history state remains unknown.
- The socket's construction and lazy bind instructions are real; successful
  runtime construction/start/bind and an authorized reachable client are separate.
- No application-layer authentication was found in the recovered path examined.
  This does not establish a globally unauthenticated service.
- No examined KIM19 path establishes generalized externally controlled execution.
  Static corpus coverage does not establish universal absence elsewhere.
- Yelp's local UI precedes a Yelp search request, but this is not a promise that
  launching the application produces no background platform or service traffic.
- Performance export is useful ordinary output. A success dialog alone cannot
  establish file creation, complete content or durable media writes.

No signing bypass, trust modification, credential manipulation, unsigned
installation, firmware/policy modification, live backend/recovered-endpoint
contact, diagnostic write, or vehicle-control action was performed. GitHub push
and branch-SHA verification are the only network operations used in this task.
